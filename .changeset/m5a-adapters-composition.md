---
'@proxitor/proxy-core': minor
---

M5a: hono inbound adapter (streaming, 405/413, disconnect-driven upstream abort), fetch upstream adapter, zod config schema with YAML/XDG loading, credential adapter (env/file with 600-perm checks), composition root with load-time plugin activation, and the `proxitor start` CLI.
