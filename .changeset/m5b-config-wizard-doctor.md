---
'@proxitor/proxy-core': minor
---

M5b: full CLI — root subcommands with `--version`, interactive `config wizard`
(@clack/prompts behind a PromptPort seam, atomic write, XDG default target),
`doctor` (staged config/credential/routing/activation/port checks, `--json`,
exit code 1 on any failure), clean one-line EADDRINUSE startup error, and
config-schema edge-case lock-in tests.
