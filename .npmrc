# Dependabot auto-authenticates npm.pkg.github.com. Without an explicit default
# registry, it resolves public packages (e.g. @types/node) against GitHub Packages
# and fails with ERR_PNPM_NO_MATCHING_VERSION. Pin the public npm registry for
# unscoped packages so Dependabot updates resolve correctly.
registry=https://registry.npmjs.org/
